name: sml_ci_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow>=2.0.0
      - dagshub
      - pandas
      - numpy
      - scikit-learn
      - matplotlib
      - seaborn